# p1 — Deterministic replay execution tracker

Updated: 2026-07-20

## Legend
- [x] done
- [ ] in progress / pending

## Phase 0 — Dependency and integration spike
- [x] Prove minimal extension path loads through PiHarnessSutAdapter
- [x] Confirm in-process model execution and process-mode smoke behavior
- [x] Add clock/virtualization probes to production replay paths
- [x] Capture baseline terminal output into a cell model

## Phase 1 — Deterministic replay core (active)
- [x] Add stable fixture event sort in normalizer (timestamp + insertion order)
- [x] Ensure loader timeline sorting is deterministic and idempotent
- [x] Add canonical same-timestamp scheduler ordering for fixtures vs timers vs poll
- [x] Add stable action sequence IDs per scheduler step
- [x] Expose ordered sequence IDs on every replay frame
- [x] Add tests for mixed same-time fixture/timer order and sequence IDs
- [ ] Add checkpoints and deterministic replay restart behavior
- [ ] Implement import sandbox + path-safety + redaction manifest
- [ ] Add frame-level event normalization details for artifact/notification/render actions
- [ ] Add comprehensive JSON/text snapshot determinism proof harness (100-run check)

## Phase 2 — Semantic state and recovery
- [ ] Add production-state observer contract
- [ ] Add semantic recovery assertions for notifications, receipts, artifacts, stale/failed flows
- [ ] Ensure duplicate suppression behavior is explicit in state processing

## Phase 3 — Terminal capture
- [ ] Add ANSI/text/cell capture and stable normalization metadata
- [ ] Record cursor overflow/collision and screenshot artifacts
- [ ] Expose capture capability status (PTY/PNG optional/unavailable)

## Phase 4 — Matrix runner and CI
- [ ] Add width/theme matrix execution and stable snapshot manifests
- [ ] Add optional conformance suite gating when optional dependencies are unavailable
- [ ] Split fast semantic suites from optional terminal suites

## Phase 5 — Inspector
- [ ] Step through replay session graph with frame search and checkpoint/time jumps
- [ ] Add output saves and screenshot capture from inspector workflow

## Exit gates
- [x] `npm run check` baseline tooling updated and runnable
- [ ] Byte-identical JSON/text output across repeated deterministic runs
- [ ] Mixed same-timestamp fixture scenario is fully deterministic with ordered IDs
- [ ] Public `/replay`/`/screenshot` flow consumes sequence metadata consistently
